apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-ros-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/name: {{ .Chart.Name | quote }}
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
data:
  robotek-ros-health.json: |-
{{ .Files.Get "dashboards/robotek-ros-health.json" | nindent 4 }}
