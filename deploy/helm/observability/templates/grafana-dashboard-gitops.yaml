apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-gitops-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/name: {{ .Chart.Name | quote }}
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
data:
  robotek-gitops-overview.json: |-
{{ .Files.Get "dashboards/robotek-gitops-overview.json" | nindent 4 }}