# Default values for the Robotek observability wrapper chart.
# Environment-specific settings belong in values-staging.yaml.

monitoring: {}

argocdMetrics:
  enabled: true
  namespace: argocd
  release: argocd
