apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: robotek-staging
  namespace: argocd

  labels:
    app.kubernetes.io/name: robotek
    app.kubernetes.io/part-of: robotek
    environment: staging

spec:
  project: default

  source:
    repoURL: https://github.com/iheb-mrabet/robotek-1.2.git
    targetRevision: main
    path: deploy/helm/robotek

    helm:
      releaseName: robotek-staging
      valueFiles:
        - values-staging.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: robotek-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true

    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m