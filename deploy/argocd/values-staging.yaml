configs:
  cm:
    url: ""

dex:
  enabled: false

controller:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

server:
  service:
    type: ClusterIP

  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

repoServer:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

applicationSet:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

notifications:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
